===========================================
TESTING GUIDE - Doctor Profile Updates
===========================================

📋 STEP-BY-STEP TESTING INSTRUCTIONS

┌─────────────────────────────────────────┐
│  STEP 1: Start the Application          │
└─────────────────────────────────────────┘

Terminal 1 - Backend:
  cd backend
  node server.js
  
  ✅ Should see: "Server running on port 5001"
  ✅ Should see: "MongoDB connected"

Terminal 2 - Frontend:
  cd frontend
  npm run dev
  
  ✅ Should see: "Local: http://localhost:8080"

┌─────────────────────────────────────────┐
│  STEP 2: Login as Doctor                │
└─────────────────────────────────────────┘

1. Open browser: http://localhost:8080
2. Click "Login" in navbar
3. Enter credentials:
   Email: karan.doctor@gmail.com
   Password: Doctor@123
4. Click "Sign In"

✅ Should redirect to: /dashboard/doctor

┌─────────────────────────────────────────┐
│  STEP 3: Navigate to Profile            │
└─────────────────────────────────────────┘

Option A: Click "Profile" in sidebar
Option B: Go directly to: http://localhost:8080/dashboard/doctor/profile

✅ Should see:
   - Doctor's name with "Dr." prefix
   - Avatar with initials
   - Specialization and Department badges
   - Professional Details card
   - About section
   - Professional Stats card (sidebar)
   - "Edit Profile" button

✅ Should NOT see:
   - Book Appointment button
   - Reviews & Ratings section
   - Quick Actions section
   - Rating stars

┌─────────────────────────────────────────┐
│  STEP 4: Test View Mode                 │
└─────────────────────────────────────────┘

Verify all fields are displayed correctly:
  ✅ Name: "Dr. [Doctor Name]"
  ✅ Email: Shows in contact card
  ✅ Phone: Shows in contact card
  ✅ Specialization: Badge format
  ✅ Department: Badge format
  ✅ Qualification: With graduation cap icon
  ✅ Experience: With award icon
  ✅ Available Days: In professional details
  ✅ Available Timings: In professional details
  ✅ About: Professional summary text
  ✅ Stats: Patients, Publications, Awards

┌─────────────────────────────────────────┐
│  STEP 5: Enter Edit Mode                │
└─────────────────────────────────────────┘

1. Click "Edit Profile" button

✅ Header should show:
   - "Edit your professional profile" text
   - "Cancel" button (gray)
   - "Save Changes" button (primary color)
   - "Back to Dashboard" button hidden

✅ Fields should transform to inputs:
   - Name: Text input
   - Specialization: Text input
   - Department: Text input
   - Qualification: Text input
   - Experience: Text input
   - Available Days: Text input
   - Available Timings: Text input
   - Email: Email input
   - Phone: Text input
   - About: Textarea (large)

┌─────────────────────────────────────────┐
│  STEP 6: Test Editing Fields            │
└─────────────────────────────────────────┘

Try editing these fields:

1. Change Name:
   Example: "Karan Kumar" → "Dr. Karan Kumar Shah"

2. Update Phone:
   Example: "" → "+91 9876543210"

3. Modify Specialization:
   Example: "Cardiologist" → "Senior Cardiologist"

4. Update Available Days:
   Example: "Monday - Friday" → "Monday - Saturday"

5. Edit About section:
   Add more details about experience

✅ All fields should accept input
✅ Cursor should move correctly
✅ Text should be visible as you type

┌─────────────────────────────────────────┐
│  STEP 7: Test Cancel Functionality      │
└─────────────────────────────────────────┘

1. Make some changes to fields
2. Click "Cancel" button

✅ Should happen:
   - All changes discarded
   - Original data restored
   - Return to view mode
   - "Edit Profile" button visible again

┌─────────────────────────────────────────┐
│  STEP 8: Test Save Functionality        │
└─────────────────────────────────────────┘

1. Click "Edit Profile" again
2. Make changes to multiple fields
3. Click "Save Changes"

✅ During save:
   - Button shows "Saving..." with spinner
   - Button is disabled
   - Cannot click Cancel

✅ After successful save:
   - Success toast appears: "Profile updated successfully"
   - Returns to view mode
   - All changes are visible in view mode
   - "Edit Profile" button appears again

✅ If error occurs:
   - Error toast appears with message
   - Stays in edit mode
   - Can try again or cancel

┌─────────────────────────────────────────┐
│  STEP 9: Verify Data Persistence        │
└─────────────────────────────────────────┘

1. After saving changes, refresh the page (F5)

✅ Should see:
   - All saved changes still visible
   - Data loaded from backend
   - No data loss

2. Logout and login again

✅ Should see:
   - Changes persist across sessions
   - Data saved in MongoDB

┌─────────────────────────────────────────┐
│  STEP 10: Test Edge Cases               │
└─────────────────────────────────────────┘

Test 1: Empty Fields
  - Clear a field (e.g., phone)
  - Save
  - ✅ Should save with empty value

Test 2: Email Validation
  - Try invalid email format
  - ✅ Backend should reject

Test 3: Duplicate Email
  - Try changing to existing user's email
  - ✅ Should show error: "Email already in use"

Test 4: Network Error
  - Stop backend server
  - Try to save
  - ✅ Should show error toast

Test 5: Rapid Clicking
  - Click Save Changes multiple times
  - ✅ Should only save once (disabled during save)

┌─────────────────────────────────────────┐
│  EXPECTED RESULTS SUMMARY               │
└─────────────────────────────────────────┘

✅ NO Book Appointment button
✅ NO Quick Actions section
✅ NO Reviews & Ratings section
✅ Edit Profile button WORKS
✅ All fields are editable
✅ Cancel discards changes
✅ Save persists to database
✅ Toast notifications appear
✅ Loading states work
✅ Data persists after refresh
✅ Cleaner, focused layout

┌─────────────────────────────────────────┐
│  TROUBLESHOOTING                        │
└─────────────────────────────────────────┘

Issue: "Authentication Required" error
Fix: Clear localStorage and login again

Issue: Changes not saving
Fix: Check backend console for errors
     Verify MongoDB is running

Issue: Fields not editable
Fix: Make sure you clicked "Edit Profile"

Issue: Toast not appearing
Fix: Check browser console for errors

Issue: Blank page
Fix: Check frontend console
     Verify backend is running on port 5001

